{
  "name": "btc-converter-monorepo",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "npm -w web run dev",
    "build": "npm -w web run build",
    "lint": "npm -w web run lint || exit 0"
  }
}
